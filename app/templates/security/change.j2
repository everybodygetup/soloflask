{% extends 'base.j2' %}
{% from 'macros/form.j2' import base, btn, render_field_errors %}
{% set title = 'Смена пароля' %}
{% block content %}
<section class="jumbotron">
  <div class="container">
    <div class="jumbotron__wrapper">
      <h3 class="main__title">Изменение пароля</h3>
      <form class="form form--security" action="{{ url_for_security('change_password') }}" method="post"
        name="change_password_form">
        {{ change_password_form.hidden_tag() }}
        {{ render_field_errors(change_password_form.csrf_token) }}
        {{ base(change_password_form.password) }}
        {{ base(change_password_form.new_password) }}
        {{ base(change_password_form.new_password_confirm) }}
        {{ btn(change_password_form.submit, value='Сменить пароль') }}
      </form>
      <footer class="main__footer">
        <p class="main__text"><a class="link" href="{{ url_for_security('login') }}">Вход</a></p>
      </footer>
    </div>
  </div>
</section>
{% endblock %}
